<van-notice-bar wx:key="this" wx:for="{{notice}}"
  scrollable="false"
  text="管理员所在食堂和窗口"
  backgroundColor="#ffffff"
  color="#000000"
/>

<van-row wx:key="this" wx:for="{{row}}">
  <van-col span="8" style='white-space:pre;'>***               ***               ***               ***</van-col>
</van-row>

<van-row wx:key="this" wx:for="{{row}}">
  <van-col span="8" style='white-space:pre;font-size: 12px;'>实时午餐订单  午餐订单金额  实时完成订单  晚餐订单金额</van-col>
</van-row>

<view wx:key="this" wx:for="{{situation1}}">
    <van-card 
      title="{{ item.situation }}"
    >
    </van-card>
</view>

<view wx:key="this" wx:for="{{situation2}}">
    <van-card 
      title="{{ item.situation }}"
    >
    </van-card>
</view>